import recommendedBump from 'conventional-recommended-bump';
import config from './lib/conventional-bump-config';

export default function bump() {
  return Promise.reject(new Error('Not implemented'))
}
